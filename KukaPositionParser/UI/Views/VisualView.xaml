<UserControl x:Class="KukaPositionParser.UI.Views.VisualView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KukaPositionParser.UI.Views"
             mc:Ignorable="d" 
              xmlns:h="http://helix-toolkit.org/wpf"
             d:DesignHeight="450" d:DesignWidth="800"
               xmlns:vm="clr-namespace:KukaPositionParser.UI.ViewModels"
    d:DataContext="{d:DesignInstance Type={x:Type vm:MainViewModel}}"
             >
    <Grid>
        <h:HelixViewport3D InfiniteSpin="{Binding Options.InfinateSpin}" ShowFrameRate="{Binding Options.InfinateSpin}">
            <h:DefaultLights/>
            <h:PointsVisual3D Color="Red" Size="6" Points="{Binding Locations}"/>
        </h:HelixViewport3D>

    </Grid>
</UserControl>
